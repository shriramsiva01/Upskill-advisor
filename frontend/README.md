The simple UI lists names of pre-defined candidates along with the target job role which they would like to attend. 
Upon clicking recommendations the API does the magic with RAG and LLM by returning the suggested courses to reach target goal.
